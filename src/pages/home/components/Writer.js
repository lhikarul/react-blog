import React from 'react';
import {WriterWrapper} from '../style';

class Writer extends React.Component {
    render () {
        return (
            <WriterWrapper>student homework</WriterWrapper>
        )
    }
}

export default Writer;